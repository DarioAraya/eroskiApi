<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
  <div class="container">
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <form
      action="/search"
      class="d-flex form-inline position-relative my-2 d-inline-block"
    >
      <input
        class="form-control me-2"
        type="search"
        name="name"
        placeholder="&#191;Qu&#233; est&#225;s buscando?"
        aria-label="Search"
      />
      <button class="btn btn-search position-absolute" type="submit">
        <i class="icon ion-md-search"></i>
      </button>
    </form>
    <div class="d-flex justify-content-end">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <a
              class="nav-link dropdown-toggle"
              href="#"
              id="navbarDropdown"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              Ordenar por
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              
              <% for(let arr of data3) {%>
                <li>
                  <% if(arr == "nombreA"){ %>
                  <a class="dropdown-item" href="/sort?sortBy=<%= arr %>"
                    >Nombre(A-Z) <% } %>
                  </a>
  
                  <% if(arr == "nombreD"){ %>
                  <a class="dropdown-item" href="/sort?sortBy=<%= arr %>"
                    >Nombre(Z-A)<% } %>
                  </a>
  
                  <% if(arr == "precioA"){ %>
                  <a class="dropdown-item" href="/sort?sortBy=<%= arr %>"
                    >Mayor Precio<% } %>
                  </a>
  
                  <% if(arr == "precioD"){ %>
                  <a class="dropdown-item" href="/sort?sortBy=<%= arr %>"
                    >Menor Precio<% } %>
                  </a>
                </li>
                <% } %>
              <li><hr class="dropdown-divider" /></li>
              <li>
                <li><a class="dropdown-item" href="/">Quitar el filtro</a></li>
              </li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
